version: "3.8"

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: fairlens-backend
    ports:
      - "8000:8000"
    environment:
      - DEBUG=false
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped
